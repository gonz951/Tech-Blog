<div>
    <div class="post">
        <h2>{{name}}</h2>
        <p class="madeby">Posted by {{user.name}} on {{format_date date_created}}</p>
        <p>{{description}}</p>
    </div>

    <button>Comment!</button>
</div>