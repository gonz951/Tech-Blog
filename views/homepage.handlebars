{{#each posts as |post| }}
<div>
    <h2>
        <a href="/post/{{post.id}}">{{post.name}}</a> 
    </h2>
    <p>
        Posted by {{post.user.name}} on {{post.date_created}}
    </p>
    <p>
        {{post.description}}
    </p>
</div>
{{/each}}